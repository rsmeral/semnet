for i in $(seq 1 6) ; do
  relpath=$(for j in $(seq 1 $i); do echo -n "../" ; done )
  # echo $relpath
  find . -maxdepth $i -name "*.html" | xargs sed -i "s%http://rsmeral.github.io/semnet/%$relpath%g"
done
